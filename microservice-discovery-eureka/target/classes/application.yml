server:
  port: 8761
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    #下面两行作用：只能将其作为eureka server，不能将其作为eureka client
    register-with-eureka: false
    fetch-registry: false
